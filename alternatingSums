function alternatingSums(a) {
    var team1 = 0;
    var team2 = 0;
    var result = [];
for(var i = 0 ; i< a.length ; i++){
    if(i%2 === 0 ){
        team1 += a[i];
    }else {
         team2 += a[i];
    }
}
    result.push(team1);
    result.push(team2);
    return(result);
}
